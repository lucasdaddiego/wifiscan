# Build artifacts (the release binary is deployed to ~/.bin, never committed)
/wifiscan
.build/
_CodeSignature/
*.dSYM/

# Machine-local Make overrides (e.g. a personal code-signing identity)
Makefile.local
